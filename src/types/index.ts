export interface NavItem {
  label: string;
  href: string;
}

export interface DomainItem {
  icon: string;
  title: string;
}

export interface CareerPath {
  title: string;
  description: string;
  icon: string;
}

export interface EducationPath {
  title: string;
  icon: string;
}

export interface VcapHighlight {
  title: string;
  description: string;
  icon: string;
}

export interface CollaborationType {
  title: string;
  icon: string;
}